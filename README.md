# 苍云 Wiki

## 自动打开开发服务

使用 VS Code 打开这个项目文件夹时，项目会自动运行开发服务器，终端里会显示可点击的网页链接：

- `Local` 是本机访问地址。
- `Network` 是同一局域网内其他设备可用的地址。

首次打开项目时，如果 VS Code 询问是否允许自动任务，请选择“允许并运行”。

也可以随时手动启动：

```bash
npm run dev
```

停止服务时，在运行服务的终端中按 `Ctrl+C`。
