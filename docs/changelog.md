# 更新记录

## 2026-08-05

### 补记 07-23 至 08-03 的八个提交，并订正两段过时注释

更新记录此前止于 07-22，其后八个提交未录：07-23【于林之下】五章与印章十二人、07-24 滚轮缩放与印章六人、07-31 定场／序、08-03 宣纸肌理。今一并补齐（见下各节），数字均自当前代码与提交实测，非转录提交信息。

顺带订正 `index.css` 中两段已与代码脱节的注释——皆因功能由「试点」转「全量」时，JSX 一侧的说明改了、CSS 一侧漏改：

- **宣纸肌理段的段首**：仍写作「單人物：馬翊」「皆以 `c.id === "mayi"` 為條件」，而 08-03 改版后判定已恒真、遍及全站 120 人。同段又称小卡「不加綾邊——9px 窄條在 ~300px 寬的小卡上佔比過重」，实则绫边正是那次自详情卡撤下、移交小卡承接的，现宽 11px；两卡所辖的效果各是哪些，一并按现状重列。另有一处引用旧函数名 `ttBandVars`（今名 `ttVars`），及 `::after` 与 z-index 两处描述只合详情卡、未提小卡走 `z-index: -1` 的另一路，均已补明。
- **SVG 印章段**：仍写作「單人物試驗：吕定仪」，实则 07-24 起全站有 `pin` 者 47 人尽走此式。
- 二者皆纯注释，未动一条规则；`npm run lint` 無錯、警告仍 12 基线，`npm run build` 通过，产物与改前一致。

## 2026-08-03

### 人物卡宣纸肌理：全站大小卡片改版，小卡版式重构

网格小卡与详情卡加纸纹与斑驳晕染，小卡版式一并重构。**零图片**：五幅 `feTurbulence` 噪点全写作 `data:image/svg+xml` 内联进 `index.css`，未新增任何资源、`public/` 一字未动；`index.css` 由 18.8 KB 增至 31.6 KB，构建后 CSS 共 13.5 KB（gzip 2.8）。

- **纸纹三层**：细颗粒（240 tile）、中频云絮（320）、长纤维（400），层间以 `overlay`／`soft-light` 相融，整体再 `mix-blend-mode: overlay` 压上卡面。逐层透明度**烘进 SVG 内 `rect` 的 `opacity`**——单元素多值背景无法逐层设 CSS `opacity`，故前移至 SVG。各幅显式设 `stitchTiles="stitch"` 与 `x/y/width/height`，否则平铺接缝处可见规则格线。长纤维一层 `baseFrequency` 横低竖高（`0.012 0.25`），拉出近水平长丝，即宣纸的簾纹走向。大小两卡共用此三幅，小卡的减配只靠整体压到六成，不另备低透明度的 SVG——纹理尺度本应与卡片大小无关，一缩小便成另一种纸。
- **斑驳晕染三圈**，右上那圈的晕色随门派而变（`--tt-stain`）。原取全站高亮橘红 `#B5442D`——那本是通用强调色、非门派色，只是恰与天策相得；苍云一入范围，冷铁灰的卡上凭空一团暖红。故改自派色派生，透明度自 0.06 提至 0.07（门派色多较原色为暗，同透明度下浮色更浅）。
- **小卡的门派色条由顶部 3px 横条改为左侧 11px 绫边**，底色自 `fc(belong[0])` 压暗三档注入（`--tt-band-a/b/c`）。两处易错：边框宽须**归零而非抹透明**——`overflow: hidden` 裁在 padding box，留着边框带则绫边顶端缺 3px 一截；噪点须按「11px 窗口看 120/80px 噪场」取样，否则横向压缩成假竖纹。归零让出的 3px 由上内距补回（16 → 19），故盒高与内容基线同邻卡逐像素一致。
- **不新增节点**：容器仅得 `::before`／`::after` 两个伪元素，纸纹与斑驳各占其一。小卡另设 `isolation: isolate` 自成层叠上下文，`z-index: -1` 方落在卡面之上、正文之下，否则负层级被推到卡片背景之后而全然不可见；如此正文一字未动。详情卡走另一路——那边正文层本就有 `z-index: 1`，伪元素用 0 即可。
- **小卡版式重构**：字号自竖排左栏改为横缀名后，名号行下以渐变金线分隔；线下生卒／出身／事件三行标目，仿详情卡「檔案」区体例（左标目定宽、右值自适应，字级与行距按小卡收紧）。原先挤作一行、以「·」相隔的写法，遇程凯这等籍贯带括注者即折成三四行而失序。值为空仍留行，使名单内诸卡结构齐一。竖栏撤去后由正文栏 `min-height: 88` 接手撑高。
- **详情卡**：纸纹、斑驳、「檔案」行线左端泛金。名号行下的金线**仅无称号者挂**——有称号者其下本就自带一道下划线，那才是主分隔线，两线相距三十余像素，并出则刺目。
- **范围由 `isTextureTest()` 一处把关，现恒真**（全站 120 人，门派为空者——程钧——落 `fc` 的中立灰回退）。留作单一开关：日后若要收回某派某人，改此一处即可，大小两卡与 `index.css` 诸规则悉听其令。
- 验证：`npm run lint` 無錯、警告仍 12 基线未增，`npm run build` 通过。

## 2026-07-31

### 【生逢】【思停】补定场／序

两线的定场与序此前皆空，阅读器只显「正文待錄」。今各补两篇，另补【登楼】定场词的出处标注。

- **【生逢】**：定场录鲁米与巫鸿《豹迹》两则题辞（122 字）——线题「大寺烈焰冲天」即出后者。序录岚川云夜遇明教一节（1612 字）。
- **【思停】**：定场录王维、令狐楚《少年行》两组各四首（315 字）；序录程凯卧病一节（2229 字）。
- **【登楼】**：定场词补出处一行「敦煌遗书《云谣集杂曲子》」，正文未动。
- **结构侧只改两行**：两线的 `stdChapters()` 各补 `dingchang`／`xu` 两个 `file` 栏。正文由 `texts.js` 的 glob 自动收录，别无他动——正合 `index.js` 开头所记的「建 txt → 掛上 file 即可」。
- 全库正文由 99.9 KB 增至 **112.7 KB**。

## 2026-07-24

### 三图缩放：新增滚轮／触控板捏合，按钮步进 1.5 → 1.3

行星图、群像图、舆地图此前只能按 ＋／－ 逐级缩放、滚轮无效。今于 `usePanZoom` 内加一路**以游标为锚的连续缩放**，三图共用。

- **走原生 non-passive 监听**，不用 JSX 的 `onWheel`：React 19 在根节点挂 `wheel` 为 passive，回调内 `preventDefault` 无效、页面照滚不误。故以 `wheelElRef` 取幅面元素，`addEventListener("wheel", …, { passive: false })` 自挂自卸。
- **1× 时不夺页面滚动**：静止时普通滚轮径直让位（`v.k === 1 && !e.ctrlKey` 即 return），划过图幅仍可翻页；唯触控板捏合（浏览器合成 `ctrlKey`）可于原始大小起手放大——同 Google 地图之例。已抵夹限者（`k2 === v.k`）亦不拦截，缩放到头自然接回页面滚动。
- **捏合与滚轮分授系数**（0.01／0.0018）：捏合 delta 细碎、滚轮成块，同系数下手感悬殊。另按 `deltaMode` 归一（行 ×16、页 ×幅高），免得以行为单位者一格跳半屏。
- **缩回 1× 即连平移一并复位**，否则图幅停在原始大小却偏在一侧。
- **＋／－ 步进由 1.5 改 1.3**：宽屏上点一下常过冲。

### 印章新增六人，并清 07-22 挂账

林叶矢（渡）、程广（刚）、关山（夷）、马翊（庄）、夏明沧（秀）、沈含英（蔼）。六人原 `pin` 皆空，故同为**新增**而非替换，累计 **47 人**。

- **内含 07-22 记下的那笔挂账**：马翊、程广二人入库时 `pin` 暂空、不出印章亦不参与品评字检索，待作者定字；今字既定，照既有两处联动（`cangyun-data.js` 补 `pin` + `seals.js` 登记）补齐，挂账清讫。
- **至此全站 120 人中有 `pin` 者恰 47，与 SVG 印章一一对应**，无一人再落在文字印章式上。`Seal` 的文字回退路径仍在（`SEAL_SVG_FILE` 未命中即用），只是暂无人走。
- 六张图红面路径（`class="st0"` 的 `d`）与模板逐字节相同，沿用小印 1.15、大印 1.20，未逐张调参。

## 2026-07-23

### 【思停】新增卷【于林之下】，五章正文

【思停】线下增一卷，五章共 **18557 字**（3577／2739／2925／4238／5078），系李庭深、程凯、魏谦一支。三人在全库正文中分别现 98／83／108 次，其中 97／67／107 次落在这五章之内；另项旌 27 次、岚川云 11 次。

- **结构无须新写**：卷（与诸章同级、其下再分章）这一层 07-21 已为【生逢·门墙】铺过，`FLAT` 展平与哈希路由早已支持。本次只在 `index.js` 加一个 `{ id: "yulinzhixia", chapters: [...] }` 节点、五个 `file` 栏一挂即成，阅读器、目录、翻页、检索一行未动。
- **正文体量逾倍**：全库由 45.2 KB 增至 **99.9 KB**，`texts` 块随之变厚。首屏主包不受影响——正文两日前已移出（见 07-21 C2）。「見於文庫」计数走构建期预算，新章自动计入，档案面板仍不拉正文。

### 印章续增十二人（三批）

祝红蕖（英）、柳戎（豪）、骆文璧（蔚）；朱鹭（容）、陈瑞和（恭）、江蓠（慧）、贺兰（耘）、岚川云（识）、茹锦（适）；楼谊（凌）、曼冉（灵）、廖群（璞）。十二人原 `pin` 皆空，同为**新增**而非替换，累计 **41 人**。

- **文件名与 id 不同形，再添一例**：祝红蕖 id 为 `zhuhongqu`，印章文件却名 `zhuhongqv-ying.svg`，承许秋生（`xuqiusheng` → `xvqiusheng-yi.svg`）的旧例，拼音以 v 代 ü。故映射表仍显式列举、不由 id 拼路径，`check-data` 比对文件存否兜底。
- **同音不同字者两对**（楼谊「凌」与曼冉「灵」、岚川云「识」与茹锦「适」）：文件名以「人名拼音 + 品评字拼音」构成，人名一段既已相异，同音不致撞名。
- 十二张图红面路径与模板逐字节相同，沿用小印 1.15、大印 1.20，未逐张调参。

## 2026-07-22

### 新增人物二人：马翊（天策）、程广（唐军）

均系作者所给填位内容入库，人物 **118 → 120**、关系 **216 → 224**。二人 `pin`（一字品评）暂空，故不出印章、亦不参与品评字检索——俟作者定字后再照既有两处联动（`cangyun-data.js` 补 `pin` + `seals.js` 登记）接印章。

- **马翊（字图南，721–757，天策）**：东汉伏波将军马援之后，程凯、李庭深的师兄。官职身份作者留空，故 `profile` 八栏全空；所给两句台词原样落入个人轶事，757 年秋破阵身亡一节入高光。
- **马援一线，经作者确认后留笔**：程凯档案「偶像」栏所奉正是马援（「男儿当死于边野，马革裹尸而还」），而马翊系马援之后、又是程凯师兄——此系交叉比对既有库所得，非作者原文，故先请示、得作者确认后方以「按」注入马翊轶事，未改程凯档案。
- **程广（字千里，唐军）**：程凯生父。作者只给「战亡西北」，而既有库中两处已系其亡年——程钊档案作「安史乱前某一年与父程广一同战亡北庭」，747 年编年条作「程凯生父程广亡于北庭——北庭与长安，同一年」——故 `death: 747`，并于档案内以「按」注明来源，`birth` 仍空。
- **两处未定内容均以「按」标注**，沿用库中既有体例（如「俟作者定」「两说并存」），不与作者正文混同：马翊死事作者原文即作「设想是」，故高光末缀「（按：设想，俟作者定。）」。
- **清掉一处挂账**：彩瑶档案原注「与天策府马翊有情（马翊档案待建，关系俟其建档后接入）」，今删括注、补 `mayi—caiyao 有情` 关系边。另补马翊与程凯／李庭深的同门·师兄两边，及程广与五子（凯、锋、钊、铎、钧）的亲缘五边——否则程广在关系图中会是一个孤点。
- **文库批注自动生效但暂无实效**：词条自 `CHARACTERS` 全量派生，二人入库即 118 → 120 条；然正文中「马翊」「程广」当前出现 0 次，故正文观感逐字不变，二人无 `readerNote` 亦不触发 check-data 的半接入警告。
- **另有作者文字修订一处**（与新增人物无关，随本次一并提交）：廖群「性格」栏删去末句「不哭也不闹。」。
- 验证：`check-data` 無錯，lint 警告仍 12 基线未增，`npm run build` 通过。

## 2026-07-21

### 正文按需加载，移出首屏主包（C2）

此前整部小说正文经 `?raw` 静态导入、构建期内联进那**一个主 JS 包**——任何人打开首页（哪怕只看关系图、从不进文库）都得先下载全部正文。现改为懒加载：正文切成独立块，**只在进入文库阅读器时才下载**。主包 **625.9 → 581.5 KB**（gzip 244 → 221），正文 46.6 KB 移入单独的 `texts-*.js`。

- **三个正文消费方，各自安置**。正文全量原本喂着三处：阅读器正文显示、目录每章「N 字」、文库全文检索——**外加**人物档案的「見於文庫」计数。前三者同处阅读器,故把正文整体切成一个懒加载块（`src/novel/texts.js`，`import.meta.glob` eager 收 txt，仅经 `loadTexts()` 动态 import 触及，Vite 遂切块），进文库时一次载入，其余逻辑不动。
- **档案计数走构建期预算，不碰正文块**。「見於文庫」在人物/关系视图，若也去拉正文，浏览人物那条路就白白下载整部书。故新增 Vite 插件 `virtual:novel-mentions`：构建期直接读 txt、按本名精确计数（与旧 `mentionGroups` 同一 while 循环：非重叠、步进本名长），产出微小映射 `{ 檔名: { 人物id: 次数 } }`。档案面板改读它——**仍不需正文即秒出，计数逐字不变**（高垣门墙各章 33/44/41/37，与改前一致）。
- **结构与文本解耦**。`src/novel/index.js` 章节改携 `file` 栏（相对 src/novel 的檔名）、不再静态 import 正文；`file` 是正文块与计数插件的共用键。
- **阅读器加载态**。挂载即 `loadTexts()`，四处取字（正文体、目录字数、章头字数、全文检索）统一走 `textOf`；正文未就位时正文区显「正文载入中」。滚动位置恢复改为**等正文就位再定位**，否则页面尚矮、`scrollTo` 够不到目标处。
- 唯一可见的新行为：**首次点进文库有一瞬加载态**——移出首屏的必然代价。人物档案面板零感知。
- 验证：主包不含正文、正文块含之（正文散文探针主包 0／文本块 1 命中）；lint 警告仍 12 基线未增、check-data 無錯；dev 实测阅读、翻页、检索、旁注、档案计数俱常。

### 印章新增十三人（首次出现，非替换）

此前接入的均为「替换」——人物本有 `pin`（一字品评），文字印章已在显示，改挂 SVG 而已。本轮十三人**原 `pin` 为空、本不显印章**，是首次出现印章，故性质是新增。分四批：程凯（烈）、齐晟（果）；程锋（景）、李庭深（穆）、程铎（严）、程钊（贞）；李庭泽（怀）、杜徵（皎）、岑菡（雅）、卢秉毓（思）；许秋生（翼）、景惠时（懿）、魏谦（捷）。累计 **29 人**走 SVG 印章。

- **改动两处联动，缺一即错**：`Seal` 取字自 `c.pin`，`pin` 又是详情页印章的显示开关（`c.pin && …`），空值时列表与详情都不出印章。故除在 `seals.js` 登记外，须在 `cangyun-data.js` 给这十三人补 `pin`。二者必须同改——只补 `pin` 不登记会冒出文字印章，只登记不补 `pin` 则 `Seal` 直接返回 `null`、`check-data` 报错。
- **`pin` 的两个副作用均属预期**：一是这十三人从此可按品评字（如「懿」「捷」）站内检索，与既有诸人一致；二是它作为隐藏字撑起 SVG 占位盒，但任一 CJK 字等宽等高，故不影响排版，`SEAL_DENSE_CH` 五个繁复字亦无一在内。
- **品评字来源与一处确认**：多数由上传时直接给定。程锋一批四人当时只随文件名附了拼音（jing／mu／yan／zhen），而一字品评系作者所定、且落为**不可见的隐藏字**（页面只见印面图，看不见 `pin`），事后无从目测校验，故逐字确认后方落库——景／穆／严／贞。其中 jing 依「镇梁州御京师」本欲取「靖」，实为「景」，幸先问过。
- **文件名与 id 不同形，再添一例**：许秋生 id 为 `xuqiusheng`，印章文件却名 `xvqiusheng-yi.svg`（拼音以 v 代 ü），故映射表显式列举、不由 id 拼路径；`check-data` 比对文件是否存在兜底。
- 十三张图红面路径（`class="st0"` 的 `d`）逐字节同模板，沿用小印 1.15、大印 1.20，未逐张调参。

## 2026-07-20

### 品评印章由试点转全量（`3317ff0`）

昨日吕定仪一人的 SVG 印章试点（`e1dd763`）扩至 **12 人**：荔非承烈、闵方城、狄元恪、靳沉白、薛褚、邱鸿、边赛远、樊璋、关岳、连朔、楼谯十一人的印章图入库，余 106 人照旧走文字印章，渲染逐字节不变。

- **`Seal` 由布尔开关改为文件名参数**：原 `useSvg={c.id === "lvdingyi"}` 写死单人，现为 `svg={SEAL_SVG_FILE[c.id]}`，命中即走 SVG 式，未命中得 `undefined` 自然回落文字式。盒模型、缩放、微调一概沿用试点所定，未再改动。
- **缩放系数无须逐人再调**：十二张图红面在 100×100 viewBox 中同位同尺寸（`x 6.73..93.51`、`y 6.40..93.76`，仅占约 87%，四边各留约 6.5% 透明边），故共用一组——小印 1.15、大印 1.20，另下移 1px 对齐视觉中心。逐张验过包围盒确认一致，非目测。
- **映射表独立为 `src/seals.js`**：`check-data.js` 是 node 直跑、解析不了 JSX，正则去刮 jsx 又脆；体例照 `novel/char-terms.js`，两边各自 import 同一张表。增删一个人只改此表一行。
- **文件名与 id 不一律同形**，如荔非承烈 id 为 `lifei`，故映射表显式列举，不由 id 拼路径。

### check-data 增印章图校验

映射表指向的文件若被改名或删除，浏览器只渲染一枚破图、构建不报错——本次接入途中恰好遇上一次改名（`lifeichenglie-ba.svg` → `lifei-ba.svg`），遂补此节，比对映射表、人物表与 `public/images/seals/` 三方。

- **报错（挡下 `npm run lint`）**：文件不存在；键非有效人物 id（该人物会静默退回文字印章）；该人物无 `pin`（`Seal` 直接返回 `null`，印章根本不出现）。
- **报警告**：目录中无人引用的孤儿图，提示「上传后忘了写进 `SEAL_SVG_FILE`？」。定为警告而非错，因批量上传、逐个接入途中目录里必然有尚未登记的图，报错会挡住正常流程。
- 改名一类会同时报出错与警告——错指出映射表里的死引用，警告指出那张变成孤儿的真文件，新旧文件名并排可见。
- 五种情形均以人为制造故障实测触发过，非只写不验。

### 印章续增凌雪阁四人

高垣（砥）、高堑（锐）、宣杨（断）、宣康（济）四人的印章图入库，共 **16 人**走 SVG，余 102 人照旧文字印章。改动仅 `src/seals.js` 加四行——上午把开关改为 `svg={SEAL_SVG_FILE[c.id]}` 之后，加人本就只是加一行表项，组件与 CSS 无须再动。

缩放系数沿用（小印 1.15、大印 1.20、下移 1px），依据是取每张图红面路径（`class="st0"` 的 `d`）全串比对，四张新图与既有十一张逐字节相同，红面在 viewBox 中同位同尺寸。此法已写进 `seals.js` 注释，后续上传新图照做即可，不必逐张目测调参。

---

## 2026-07-19

**当日里程碑：站点正式上线。** 部署至 GitHub Pages —— **<https://jadexuan155229-dot.github.io/cangyun-wiki/>** ，公网公开可访问、链接可直接分享（源码仓 `jadexuan155229-dot/cangyun-wiki` 一并公开），此后 `push main` 即自动重新部署。此前项目只在本机与 zip 之间流转，至此有了对外地址。

本日各批改动均已真机／桌面验过，`npm run lint` 与 `npm run build` 皆过，分记如下。

### 三图触屏适配（`01132bc`）

行星图、群像图、舆地图此前只认鼠标，手机上不可用。三图各自一套缩放平移代码，此次抽出共用的 `usePanZoom(w, h)` 与 `ZoomBtns` 一并接入。

- **双指捏合**：多指以 `Map<pointerId>` 记账、`setPointerCapture` 接管，缩放以两指中点为锚。此前第二根手指落下会无条件覆写拖曳原点，双指操作时图幅跳变。
- **1× 时不夺页面滚动**：`touchAction` 仍是 `k>1` 才置 `none`。单指可照常划过图幅翻页，放大后才吃手势。代价是捏合放大须先按 ＋，换来的是整幅图不至于成为手机上的滚动死区。
- **悬停专属信息改点选可及**：触屏无悬停，此前有几处信息在手机上根本够不着。按 `useCoarsePointer()` 分流，桌面行为一概不变——
  - 群像图、行星图：轻触人物先显其关系边（弧），再触方开档案
  - 舆地图动线段：轻触浮出「繫此段之事」，且不转开档案（档案由驻点与人名承接）；浮框改浮于指上方，免为指腹所掩
  - 各图另加透明衬底，点空处即卸下所选
- **触屏点选域加大**：群像节点、行星节点、地点环、动线感应带在窄屏上实际只有几像素宽。
- **行星图补上缩放平移**：此前完全没有，1200×830 的幅面直接挤进手机宽度。

顺带修掉两个**既有缺陷，桌面同样中招**：

1. `movedRef`（防拖后误触的位移量）只在 `pointerdown` 里重置，而该回调在 `k===1` 时提前 return。于是「放大 → 拖动 → 按『回』复位」之后它永停在 >5，此后**一切点选**被当作拖后误触吞掉，只能刷新页面。
2. `setView` 的 updater 延后解引用 `dragRef.current`。React 19 中 `pointermove` 属连续事件被排队延后，`pointerup` 属离散事件抢先同步跑完并把 `dragRef` 置空，updater 这才执行、读 `null.tx` 抛错，整棵 React 树卸载。桌面鼠标 move 与 up 间隔大几乎撞不上，**安卓上一抬手就白屏**。改为先取快照、updater 只用局部值。

另在 `src/main.jsx` 加了开发期错误浮层（`import.meta.env.DEV` 守着，不入打包）：未捕获错误直接印在页面底部，手机上无控制台时靠它取堆栈。

### 印章与门派纹（`e1dd763`）

- 吕定仪的品评印章试点改用 SVG。`Seal` 抽出共用盒模型，SVG 与文字两式共享外框尺寸、内边距与基线；SVG 式以一枚隐藏字撑出等宽等高的占位盒，图片绝对定位铺满、不参与排版。
- 门派底纹增岑荃、唐军两种。
- `public/images/` 重整为 `faction-marks/` 与 `seals/` 两个子目录，`index.css` 路径随之更新。

### 岑荃底纹换图并瘦身

- 换用新的团花图样，并预先灰度化后转 WebP：**1.71 MB → 110 KB，小十六倍**。此纹的 CSS 本就带 `filter: grayscale(1)`，彩色信息一个像素都用不上，故预先灰度不改渲染结果（`filter` 保留不动，幂等）。原图与新图同为 1254×1254、四角纯黑，`mix-blend-mode: screen` 所需的黑底透出照旧。
- 删去从未被引用的 `cenquan-mark.webp`（64 KB）。`public/` 会被整个原样复制进 `dist`，未引用的资源照样发到线上，故须显式删除。
- 产物总体积由约 3.6 MB 降至 **1.8 MB**。

### 提交署名

此前全部提交用的是占位邮箱 `jade@example.com`（`example.com` 是保留域名，并非真实地址），GitHub 无从据此认人。已改为 noreply 匿名署名，并把既有 14 个提交的 author 与 committer 一并重写。因尚未推送至任何远程，此举无副作用；内容与重写前逐文件比对为零差异，只动元数据。

### 门派纹三图再瘦身（`8c69152`）

上线前把现存最大的三张 PNG 一并转 WebP：

- **衍天宗 393 → 56 KB、万灵 150 → 33 KB**：二者在 CSS 里作 `mask`，只吃 alpha 通道（注释所谓「mask 會壓成剪影」），故丢弃 RGB、仅存原 alpha 转 lossless WebP。alpha 逐像素与原 PNG 比对为零差异，剪影不变。
- **段氏 29 → 6 KB**：作 `background` 且本就带 `filter: grayscale(1)`，同岑荃底纹之法，预先灰度后转 WebP，不改渲染。
- 删去初始模板带入、全站零引用的社交图标 sprite `icons.svg`（bluesky/discord/github/x…）。`public/` 整个原样进 `dist`，未引用者照样发到线上。
- `dist` 由约 1.8 MB 降至 **1.4 MB**。

### 上线 GitHub Pages（`ea4ec2f`）

站点部署至项目子路径站 **<https://jadexuan155229-dot.github.io/cangyun-wiki/>**，push `main` 即经 Actions 自动 build 并发布，往后改动自动上线。

- `vite.config.js` 设 `base: '/cangyun-wiki/'`：站点不在根域，资产须带此前缀，否则从根请求全部 404。index.html、CSS `url()`、Vite 处理的资产均由构建自动重写。
- 唯一漏网是 `cangyun-wiki.jsx` 里印章 `img` 的硬编码 `src="/images/seals/…"`——JS 字符串字面量 Vite 不重写，改走 `import.meta.env.BASE_URL` 拼接。哈希路由不受子路径影响，无需 SPA rewrite。
- 新增 `.github/workflows/deploy.yml`：`npm ci` + build 后经 `actions/upload-pages-artifact` 与 `deploy-pages` 发布 `dist`；`dist` 仍 gitignore，由 CI 现构建、不入库。首轮因新仓 Pages 未启用而失败，启用（源＝GitHub Actions）后重跑 build ✓ deploy ✓，线上入口／JS／CSS／三图 webp／印章 svg／favicon 抽验皆 200、类型正确。

### og:image 分享卡

补上分享到聊天／社交时的预览卡图（`index.html` 之 `og:image`、`twitter:card`）。取站点人物档案首页截图（2511×1143），等比缩至 1200 宽后置于 1200×630 深色画布、上下衬边 42px——衬色取截图自身背景 `#15181D`（恰为 `theme-color`），标题、整条导航、门派筛选条与人物卡尽收、无一裁切。产物 `public/og.png` 149 KB。`og:image` 用线上绝对 URL，另补 `og:url`、`twitter:card=summary_large_image` 与宽高。

---

## 2026-07-18

项目由手工 zip 备份转入 git 版本控制，当日建立主要基础设施。

- **入库与校验**：站点全量入库；`scripts/check-data.js` 校验五库与词条的引用完整性，挂入 `npm run lint`。
- **哈希路由**（`src/router.js`）：页签、人物档案、文库章节写入网址，可分享、刷新不丢、前进后退可用。
- **元信息**：`index.html` 补 `lang=zh`、站名标题、description 与 OG 标签。
- **文库批注全量接入**：词条自 `CHARACTERS` 自动派生（`src/novel/char-terms.js`），118 人正文即点即出旁注卡，无须逐人加词条；识别逻辑抽为纯函数，可直接用 Node 测试。
- **文库反向索引**：档案弹窗增「見於文庫」，按「线（·卷）」两级聚合，点开见章、可直达。
- **阅读体验**：阅读位置记滚动偏移，回到文库接着上次读到处；阅读设置加字号五挡、行距三挡、玄墨／素纸两主题。
- **交互一致性**：`DetailPanel` 响应 Esc 关闭，与文库批注卡对齐。

---

## 待办

- ~~**C2 正文按章懒加载**~~：**已于 07-21 完成大半**——正文整体切出独立块、移出首屏主包，「見於文庫」则不必改异步，直接改走构建期预算（`virtual:novel-mentions`），档案面板零感知。余下的只是**按章再分块**：现全部正文合为一块，进文库时一次载入 115.9 KB（gzip 56.2）；07-21 时尚只 46.6 KB，两周内已增至两倍半，届体量难忍时再切。
- **`readerNote` 按人补写**：现 120 人中仅 1 人有此字段，其余回退为摘要，现状可用，不必赶工。

条件触发项：正文若多以表字、别号指人，则加 `aliases` 识别；同名人物入库时 `check-data` 会拦下，届时另定人工映射；人名恰为常语而被误中者，以 `char-terms.js` 的 `TERM_EXCLUDE` 摘除。
